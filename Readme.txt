To run this project you might need some modules to be imported in Python.
And to import them, you will need to install the particular version of some libraries.

tkinter
pillow library = latest
playsound = still to be checked
python-statemachine = 0.8.0
